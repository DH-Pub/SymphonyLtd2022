@model IEnumerable<Symphony.Areas.Admin.Models.About>


@{
    ViewData["Title"] = "About Index";
}

<table id="about" class="table table-bordered table-hover">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.Number)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Description)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
        @foreach (var item in Model)
        {
            <tr>
                <td>
                    @Html.DisplayFor(modelItem => item.Number)
                </td>
                <td style="max-width: 900px">
                    @Html.DisplayFor(modelItem => item.Description)
                </td>
                <td>
                    <a href="@Url.Action("Details", "About", new { id=item.Number })" class="btn btn-secondary">
                        <i class='fa fa-info-circle' aria-hidden='true'></i>
                    </a>
                    <a href="@Url.Action("Update", "About", new { id=item.Number })" class="btn btn-secondary">
                        <i class='fa fa-pencil-square' aria-hidden='true'></i>
                    </a>
                    <a href="@Url.Action("Delete", "About", new { id=item.Number })" onclick="return confirm('Do you want to delete this ?')" class="btn btn-secondary">
                        <i class='fa fa-trash' aria-hidden='true'></i>
                    </a>
                </td>
            </tr>
        }
    </tbody>
</table>


@section Scripts {
    <script>
        $(() => {
            $('#about').DataTable({
                "paging": true,
                "lengthChange": true,
                "searching": true,
                "ordering": true,
                "info": true,
                "autoWidth": false,
                order: [[0, 'desc']],
            })
        })
    </script>
}
